当前的开发目的为公司内部的已有平台❯补充新功能，公司内部代码不能外传，所以数据

 帐号管理新增帐号：
 1. 增加子帐号选项，每个子帐号包含多个API，API名称和APIkey 一一对应，且支持excel导入， 导入excel 列标签为API名称、API key；
 4. 新注册帐号，选择供应商，自动填写 base url、供应商后端网址、令牌总额接口
 5. 新增的密钥需要与之前的密钥去重，并弹出提示，已有重复密钥不要添加
 6. 密钥删除：如果已被调用，则提示不能删除
 7. 密钥管理表格，列标签支持筛选；管理员支持编辑，同时增加创建人、总额、余额列
 8. 总额和余额从供应商后端网址调总额接口和余额接口

 API申请和审核模块：
 1. 新增需求：用户需填写 需求项目、需求详情、审批人
 2. 审批人必须为API的管理员，否则弹出该成员名下没有API
 4. 审核人审核后，用户API调用界面可弹出对应的供应商、API名称、可调用模型，不暴露密钥
 
 API调用模块
1. 当前的开发目的为公司内部的已有平台补充新功能，公司内部代码不能外传，数据库为clickhouse，数据库不能外接；当前平台可以采用输入虚拟数据来导入，方便植入到公司内部平台
2. 在线使用：
a. 需要支持上下文、支持输出图显示，输入输出图尺寸高度控制在200像素；
b. 新对话的供应商、API、模型自动填充上次选择
c. 历史对话点开显示当时调用的模型，支持对话续写
d. 历史对话支持重命名和删除，点击悬浮出按钮
3. 批量调用：
a.支持选择当前平台已有的数据、也支持上传新的数据
b.支持界面配置， 点击选择数据集上传数据集，配置指令等
c.终端配置，终端可以自行叠加后处理、设计存储格式存储路径
d.批量调用的结果：显示输入输出和指令，设计成表格类型，其中图片和文本支持直接展示在表格，文件则显示连接，支持点击解析
d.批跑记录批跑历史，点解可调出批跑设置和批跑结果 


1. 合并新增帐号和和新增APIkey选项 命名为新增API按钮；
2 新增API按钮点开，呈现 供应商、base url、供应商后端网址、子帐号、总额接口、余额接口、费用接口、API名称、API key
3. API名称和API key 一一对应，放在一行，支持在线输入也支持导入excel
4. 去掉总额、余额、已用json三个字段

❯ 新增帐号改成新增供应商，去除管理员和所属团队选项
新增API key 在最下方增加管理员，管理员支持手动输入

新增供应商模块
1. 新增供应商不需要在管理界面加一行后台存储即可；相同的供应商+URL，提示已有相同供应商
2. 供应商对所有用户可见
新增APikey 模块
2. 所属帐号改为所属供应商；新增APikey APi名称 和APIkey 放到一行；
3. 批量导入选择表格后，显示请先选择子帐号
4. 新增APIkey 需要增加帐API管理员选项，默认是创建人，可修改
5. 现在模版显示失败
6. 展示表格，筛选，不要单独一行，放在表头侧边加漏斗显示
7. 编辑帐号和编辑API放一起，为编辑按钮
8. 已被调用帐号不支持删除
────────────────────────────────────────────────────────────


密钥管理 
1. 密钥管理 显示管理员列 
2. 编辑打开默认显示当前的值；支持修改
3. 现在修改后的结果没有保存，已经修改了管理员，但是在新建申请界面选择，提示他名下没有密钥
4. 管理界面可以显示自己创建的和管理员是自己的API

密钥管理集成到API申请&审核
新增供应商去除管理员，新增密钥API管理员替换为管理员
密钥审核，每个用户都可见，只要有用户申请自己名下的API
AIP申请&审核改名API管理
API申请、APi审批表格都展示申请额度，单位为元


密钥管理去掉总额接口、余额接口、已用接口；总额和余额列填写的值为总额接口和余额接口的值


❯ yutongtong有为管理员的API；但是在yutongtong登录的帐号的审核界面弹出该名下没有帐号
只有审核人可以审核帐号申请，非审核人，审核按钮设置成灰色，无法点击

APi管理的密钥管理为空；需要把侧边的密钥管理整个模块移动到APi管理的密钥管理tab 下面
密钥管理的下载模版excel 报错

去除密钥管理模块
yutongtong有为管理员的API；但是在yutongtong登录的帐号的审核界面弹出该名下没有帐号
只有审核人可以审核帐号申请，非审核人，审核按钮设置成灰色，无法点击


❯ 管理员写的是admin 但是默认变成了yutongtong； 
  新增密钥key  API名称或者key 为空，不需要计入失败
  缩小密钥管理模块新增供应商tab与密钥管理tab的距离，参考审核管理间距



  导入excel API key或者名称为空值直接跳过，不需要弹出提示
  我的申请改成API申请，审核管理改成API审核
  新增API的管理员填写的是admin 导入后还是yutongtong，请修改后自己复核是否修改正确

  新建APi填写了管理员，APi显示的管理员列还是为空
  API 审核 管理员是yutongtong ，但是yutongtong 帐号点击审批显示无权操作
  密钥管理界面，用户只能看到自己创建的和自己是管理员的APi，现在yutongtong账户可以看到admin 创建的和admin 是管理员的APi

  API调用模块，
  1. 批量处理下方新增三个tab：点击配置、终端配置、处理历史
  2. 点击配置为当前的批量处理所有内容
  3. 终端配置为脚本的所有内容
  4. 历史处理：批量调用的结果：显示输入输出和指令，设计成表格类型，其中图片和文本支持直接展示在表格，文件则显示连接，支持点击解析
  5. 去除click house 相关的数据库，用假数据方式json格式，一列存数据路径，一列存标签


API  申请界面：
1. API 审核，普通用户只能看到需要被自己审核的清单，管理员可以看到所有用户的申请清单
2. 密钥管理去掉总额接口、余额接口、已用接口
3. 只有创建人有编辑和删除权限，已被调用过的APi不支持删除
4. 新增一列使用人，使用人为此API被审核配置的API申请人员

API 调用，新建对话 一步的 API key 没过期显示已过期


API申请和APi审核界面表格都增加一列API名称，未审批通过填写NA，审批通过则填写对应的API名称

API调用：
1、历史对话，支持更换模型继续对话
2、供应商、API 名称、模型，支持输入模糊匹配
3、点击配置，APi名称为新增APIkey里的API名称
4、已审批通过API申请，在API审核界面需要显示被分配的API命名，没有审批或者审批拒绝才是NA

AP I申请
审批人支持自主输入，审批人名下可以没有密钥，但是在审批环节提示其没有密钥，新增密钥


使用统计模块：
1. 分两个tab：个人看板、平台看板，参考API管理的导航布局
2. 权限：平台管理员看平台看板，普通成员看个人看板
3. 个人看板：
1）我的概览：今日请求、今日Token、今日费用、成功率；采用顶部卡片的形式
2）我的调用：不同模型请求量、token数、调用占比，表格展示
3）我的费用：每个APi key 余额、已花费额度，表格形式


4. 使用统计 模块增加平台看板
1）顶部核心指标：今日请求量、今日Token、今日成本、活跃组织数、活跃用户数、成功率、平均响应时间，卡片形式
2）平台趋势：请求趋势、成本趋势、模型使用趋势，曲线图
3）排行：项目调用排行（项目、请求量、token、成本）、用户调用排行（项目、请求量、token、成本）表格样式
4）模型治理：模型成本排行、模型成功率、模型延迟、热门模型
5）异常监控：失败率、超时、异常增长


token 数显示规则
< 1K  直接显示数量999
< 1M 单位用K
< 1B 单位用M
>=1B 单位用B



先为这个功能编写测试，不要实现功能。
测试通过后再实现代码。
最后运行测试并修复失败。



yutongtong 帐号API调用了一步APi，但是个人看板数据都没有显示，请查找原因


APi调用：
1. 新对话，选择API名称，右侧的模型下拉支持显示更多模型
2. 对话放在中间位置，站当前画面的1/2，参考豆包的对话布局
3. 横向宽度支持拖拽


 输入区 — 保持在右侧，背景色改为 rgb(242,242,242)，内部输入行和附件行限宽 900px
    - 对话区 — 背景色同为 rgb(242,242,242)，消息内容限宽 900px 居中显示

 用户的输入— 对话框右侧，输入底色改为 rgb(242,242,242)
 模型的输出--对话框左侧，底色为白色
  整个模块 — 底色为白色，输入和输出的宽度为900px 居中显示

脚本批跑
批次ID：系统自动命名B_YYYYMMDD_HHMMSS_序号
任务名称：支持用户自定义，任务完成后支持修改，如果没有默认为NA

一、批跑历史界面：
按照表格表示，表格的列标签为批次ID、任务名称、状态、进度、chen
状态：排队中/运行中 / 已完成 / 失败 / 部分失败
进度：已处理 / 总数 + 进度条
成功数：成功的条数
失败数：失败的条数
操作：查看详情、下载结果、重试失败行

二、查看情页
1. 批次概览
任务名称、发起 / 结束时间、耗时、成功 / 失败数、总条数
配置信息：供应商、模型、Prompt 模板、输入字段
2. 明细列表（输入输出对应）
1） 列标签：行号	输入字段（图片路径 / 文本）、	输出结果	状态	耗时	错误信息
2）文本输入：如果字段多，用「字段名：值」的格式展示，如：评论内容: 这个手机拍清晰；用户问题: 对比这两款产品的差异
3）图片输入：不直接放图片，用「缩略图预览 + 路径 / 文件名列表」，鼠标 hover 可放大，点击可打开原图。多张图用逗号分隔展示，如 主图.jpg, 副图1.jpg, 副图2.jpg。
4）. 支持按状态筛选（只看成功 / 只看失败）、支持搜索输入 / 输出内容
5）支持下载结果（含输入 + 输出的完整 Excel）

三、关键交互设计
实时刷新：运行中的批次，列表页每 3 秒自动刷新进度
错误重试：对「部分失败」的批次，支持单独重试失败的行
下载控制：
运行中：只允许下载已处理部分的结果
已完成 / 部分失败：下载完整结果
状态区分：不同批次的时间、状态、结果完全隔离，不会混淆



1. 入口位置
在「批量处理」页面顶部，新增一个显眼的 **「+ 新建任务」按钮 **，和「历史任务」入口并列。
步骤 1：基础信息配置
任务名称（必填）：如 竞品图片分析-20260518-Opus，自动带时间戳后缀防重
任务描述（可选）：如 100组竞品图片描述，使用Opus模型，图片路径+自定义指令
供应商 / API / 模型（级联选择，和现有下拉框一致）
超时设置（默认 600s，可自定义）

步骤 2：输入配置
上传文件：支持 CSV/Excel，和现有逻辑一致
字段映射：
自动识别上传文件的列名（如「图片路径」「自定义指令」）
让用户勾选「作为输入字段」，支持单文本、单图、多图 + 文本混合输入
对多图场景，自动识别「图片路径 1」「图片路径 2」这类多列，标记为「图片输入组」
步骤 3：任务配置与执行
Prompt 模板：和现有一致，支持 {字段名} 引用
执行模式：「一次性执行」：跑完即结束
点击「创建并运行」，任务开始执行，自动进入「任务详情页」


处理历史改为任务列表
顶部显示任务概览：任务名、批次 ID、创建时间、模型、成功率、总耗时
中间保留你现有的「脚本编辑区」：
自动根据用户配置生成初始代码模板（如 chat_with_image 调用、字段映射）
支持用户修改脚本，修改后「保存任务」，不影响原配置
右侧「运行日志 + 结果文件」和你现有逻辑完全一致，同时新增：
「重新运行」按钮：一键复用当前配置和脚本，重新执行任务


在「批量处理」页面顶部，新增一个「+ 新建任务」按钮 ，和「处理历史」入口并列。
一、核心设计：一个任务，双模式执行
核心原则：所有操作都围绕「任务」展开，点击配置和脚本批跑，只是同一任务的两种交互方式，最终生成的都是同一份批处理任务。
用户视角流程：
新建任务 → 配置基础信息（名称、文件、字段、模型）
选择执行方式：
【模式 A：可视化配置，也就是现在的点击配置】：用表单填 Prompt，一键跑批
【模式 B：脚本编辑，也就是现在的脚本】：在自动生成的代码上自定义，再跑批
无论哪种方式，执行后都生成同一条处理历史，结果、日志统一展示

关键联动：配置 ↔ 脚本 双向绑定
配置 → 脚本：用户在「点击配置」页上传文件、选字段、写 Prompt，系统会实时自动生成适配的 Python 脚本，同步到「脚本」标签页。
脚本 → 配置：用户修改脚本后，可以点击「同步到配置」，系统会解析代码中的关键参数（如文件路径、字段名、模型），回填到「点击配置」表单。
脚本页增加「重置为配置模板」按钮：一键恢复到根据当前配置生成的初始代码。
处理历史页按任务分组：用户从任务列表进入详情页后，只看到该任务的所有批次
两个页面的「开始批跑 / 运行」按钮，最终调用同一个后端执行接口
运行时，优先使用脚本页的代码（如果用户修改过），否则使用配置自动生成的代码
脚本编辑器增加「语法检查」和「运行前校验」：提前发现代码错误，避免运行失败
任务保存时，同时保存「配置数据」和「修改后的脚本」：下次打开任务时，自动恢复用户的配置和自定义代码


1.点击配置当前的APi名称，不是很正确的值，修复
2.去除切换按钮，已建好的任务，处理历史操作栏，增加配置按钮，按钮点开，呈现当前的点击配置和脚本界面
3.支持任务暂停、续跑、重跑
4.处理历史的结果详情修改如下：
1）把日志和明细列表合并成一个表格，每一行就是一次处理记录：
行号	处理对象（图片 / 文本）	指令	结果预览	状态	耗时	操作
2）交互细节
搜索过滤：在明细列表上方增加搜索框，支持按文件名、指令、结果内容搜索，快速定位目标行
筛选功能：支持按状态筛选（全部 / 成功 / 失败），方便用户只看问题数据
一键复制：每条记录支持「复制输入 / 复制输出」按钮，方便用户调试 Prompt
错误高亮：失败的行用红色背景标记，鼠标 hover 显示完整错误信息
进度回溯：如果任务很大，支持按批次分页加载日志，避免一次性渲染大量数据导致页面卡顿


点击配置和脚本配置解耦，互不影响 
脚本界面增加任务名称选项，放在最上方，下方默认生成任务ID
脚本和点击两个模块 点击运行后，两个界面自动恢复到默认状态
点击配置和脚本配置的任务都进入到处理历史
批跑任务列表直接显示任务ID，任务ID和任务名称属于一一对应的关系，不是包含关系；处理历史增加创建时间，运行状态，进度放一层
操作有配置、结果查看、重跑、下载四个选项
配置按钮点开，如果是点击配置呈现点击配置，内容自动，如果是脚本配置就显示当时写的脚本，

1. 任务命名：推荐日期+描述
2. 同一个人任务不能重名
3. 点击配置，不能读取excel 的路径的图片，报“抱歉，我是一个AI模型，无法直接访问您本地的图片路径或外部链接，因此我无法看到您想分析的图片内容。”
4. admin 处理历史中的结果，点开未保存跑出来的结果
5. 去除处理历史的批跑任务列表字体显示
6. 点击配置改为前端配置，脚本改为脚本配置，处理历史改为任务列表

结果模块的结果列表，需要把excel 每个列标签以及输出分列展示




